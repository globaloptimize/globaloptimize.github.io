/*
  Analytics configuration — edit only this file.

  Recommended default for a faculty homepage:
  1. Create a free Cloudflare Web Analytics site.
  2. Paste its token below.
  3. Leave GA4 blank unless you need richer geography and custom-event reporting.

  These identifiers are designed to be public in client-side source code.
*/
window.ACADEMIC_SITE_ANALYTICS = Object.freeze({
  cloudflareToken: '',       // Example: '0123456789abcdef0123456789abcdef'
  ga4MeasurementId: '',      // Example: 'G-ABC123DEFG'
  requireGoogleConsent: true // Shows a compact opt-in banner when GA4 is configured.
});
